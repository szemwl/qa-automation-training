Java 17  
Maven 4  
JUnit 5  
Rest Assured 5.4.0  
JSON schema validator 5.4.0  
Jackson 2.21.1  
Lombok 1.18.30  
Selenium 4.2  
WebDriver 5.8  
SLF4J 2.0.13  
---

Задание для автоматизации: Часть 2 (Selenium)
Примечание:
- Для практики использовать сайт https://www.saucedemo.com/
- Для ui автотестов использовать паттерн Page Object (три слоя test, page, step)
- step для бизнес-логики организовывать по fluent interface
- общие компоненты лучше вынести в одну страницу
- использовать явные и неявные ожидания  
  Задания:
1) Реализовать возможность выбора разных браузеров для запуска автотестов (2-3 браузера)
2) Сделать тест на успешную авторизацию через разные браузеры
3) Добавить товар в корзину, проверить что кнопка "Add to cart" изменилась +
   счётчик корзины увеличился. Зайти в корзину - удалить товар, проверить, что счётчик обнулился
4) Добавить в корзину 3 СЛУЧАЙНЫХ товара, после каждого добавления смотреть, что счётчик увеличился.
   Зайти в корзину, увидеть, что на странице корзины их ровно 3. Проверить, что отображается их
   цена и название и кнопка Remove. Удалить СЛУЧАЙНЫЙ товар, проверить что на странице корзины
   2 товара. Вернуться на главную страницу и увидеть, что удалённый товар снова имеет кнопку "Add to cart"
5) Happy path оформления заказа (карточки на странице checkout
   должны соответствовать выбранным ранее) и в конце увидеть что корзина пуста (нет счётчика)
6) Проверить работу 4 видов сортировки
7) Проверка совпадения карточки товара с карточкой на главной странцие
8) Добавить товар в корзину и попытаться оформить товар без указания полей
9) Сделать полный бизнес-сценарий для standart и perfomance-glitch юзеров параметризированно (использовать wait)

---

Задания для автоматизации:
Подготовительный этап:
1) Создать репозиторий на github 
2) Инициализировать проект с любым сборщиком
3) Подключить базовые зависимости (Junit5, Selenium, Rest Assured, Jackson) / остальные 
фреймворки для кодогенерации по желанию
4) Организовать структуру проекта (пример) 
- api 
- ui
- data
- tests

Этап 1: Фреймворк для тестирования API: FakeRESTApi
1) 
Изучить спецификацию fakerestapi.azurewebsites.net
Создать pojo-классы для всех сущностей (для минимизации бойлерплейт кода можно использовать ломбок)
чтобы правильно парсить формат api использовать анноташки jackson
2) 
Создать класс для работы со спецификациями запросов и реализовать в нём статические методы 
для хранения url, заголовков и логирования.
Также сделать класс для спецификации респонсов, который будет проверять 
соответствующие статус коды 
3) 
Сами тесты:
- testCreateBook: через builder создаётся книга, затем проверить что id книги не Null 
и title соответствующий в респонсе 
- testGetBookById: создать книгу -> получить её по Id и сравнить что объект созданной книги 
соответствует тому, который посылался для создания книги 
- testGetAllBooks: проверить, что список книг содержит книги и не пустой
- testDeleteBook: создать книгу -> проверить что она создалась -> удалить книгу ->
проверить что она удалилась (работать не будет, просто логику проанализировать, апи стейтлесс)
- testNegativeGetNonExistingBook: запросить книгу с несуществующим ID и проверить
статус код 
- Подготовить данные для параметризации на создание книги (минимум три значения 
на каждое поле, например, жанр - детектив, фантастика, роман).
Потом необходимо создать источник данных для параметризации - должен быть метод, 
который генерирует все возможные комбинации этих параметров.
Эти комбинации будут предоставляться как входные параметры для параметризированных тестов.
Создать книгу с каждой вариацией параметров.
- Написать Json-схему на книгу. 
Сделать тест, в котором создаётся книга и валидируется json-схема ответа 
с описанной схемой


RequestSpecification.spec.
when 
Responce


